---
interface Props {
    content: string,
    className?: string,
}

const { content, className } = Astro.props
---

<div class=`px-3 py-1.5 rounded-full bg-indigo-300 font-medium text-indigo-950 leading-normal ${className}`>{content}</div>