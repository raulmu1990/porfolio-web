---
const { sectionClass, containerClass, sectionId } = Astro.props
---

<section class=`p-6 flex justify-center items-center min-h-screen snap-center z-10 lg:p-8 ${sectionClass}` id={sectionId}>
    <div class={`w-full max-w-[1076px] ${containerClass}`}>
        <slot />
    </div>
</section>